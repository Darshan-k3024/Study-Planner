<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= Tasks.title %> · StudyTrack</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex flex-column min-vh-100">
  <nav class="navbar navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/tasks">StudyTrack</a>
    </div>
  </nav>

  <main class="container py-4 flex-grow-1">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-xl-7">
        <div class="card border-0 shadow-sm mb-3">
          <div class="card-body p-4">
            <div class="d-flex justify-content-between align-items-start gap-3">
              <div>
                <h1 class="h4 mb-1"><%= Tasks.title %></h1>

                <div class="d-flex flex-wrap gap-2 mb-2">
                  <span class="badge bg-secondary"><%= Tasks.subject || "General" %></span>

                  <% let priorityClass =
                      Tasks.priority === "High" ? "danger" :
                      Tasks.priority === "Medium" ? "warning text-dark" :
                      "success"; %>
                  <span class="badge bg-<%= priorityClass %>">Priority: <%= Tasks.priority %></span>

                  <% let statusClass =
                      Tasks.status === "Completed" ? "success" :
                      Tasks.status === "In Progress" ? "info" :
                      "secondary"; %>
                  <span class="badge rounded-pill bg-<%= statusClass %>"><%= Tasks.status %></span>
                </div>
                    <h3 class="h4 mb-1"> Task Id: <%= Tasks._id %></h1>
              </div>

              <div class="text-end">
              
                <div>
                  <a href="/tasks" class="btn btn-sm btn-outline-secondary">Back</a>
                </div>
                <div>
                    <br>
                    <form action="/tasks/<%= Tasks._id %>?_method=DELETE" method="post">
                        
                        <button class="btn btn-danger btn-sm">Delete</button>
                        <script>
                            document.body.querySelector(".btn-danger").addEventListener("click",()=>{
                              alert("Do you sure..?")
                            })
                        </script>
                       
        
                  </form>
                </div>
              </div>
            </div>

            <% if (Tasks.description) { %>
              <hr />
              <h2 class="h6 text-uppercase text-muted">Description</h2>
              <p class="mb-0"><%= Tasks.description %></p>
            <% } %>

            <hr />
            <div class="row g-3 small">
              <div class="col-6 col-md-3">
                <div class="text-muted fw-semibold small">Due date</div>
                <div><%= Tasks.dueDate ? Tasks.dueDate.toLocaleDateString("en-IN") : "-" %></div>
              </div>
              <div class="col-6 col-md-3">
                <div class="text-muted fw-semibold small">Status</div>
                <div><%= Tasks.status %></div>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="border-top bg-white py-3 mt-auto text-center small text-muted">
    &copy; <%= new Date().getFullYear() %> StudyTrack
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
